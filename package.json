{
  "name": "slice-monorepo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:desktop": "cd apps/desktop && bun run tauri dev",
    "dev:landing": "cd apps/landing && bun run dev",
    "build:desktop": "cd apps/desktop && bun run tauri build",
    "build:landing": "cd apps/landing && bun run build",
    "release": "cd apps/desktop && bun run tauri build --bundles dmg && echo '\n✅ DMG 생성 완료:' && ls -lh src-tauri/target/release/bundle/dmg/*.dmg"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^6.0.0"
  }
}